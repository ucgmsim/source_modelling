[build-system]
requires = ["setuptools", "setuptools-scm", "cython", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "source_modelling"
authors = [
    {name = "QuakeCoRE" },
]
description = "Source modelling library"
readme = "README.md"
requires-python = ">=3.9"
dynamic = ["version", "dependencies"]


[project.scripts]
plot-srf-moment = "source_modelling.scripts.plot_srf_moment:main"
plot-srf-cumulative-moment = "source_modelling.scripts.plot_srf_cumulative_moment:main"
plot-srf = "source_modelling.scripts.plot_srf:main"
plot-srf-rakes = "source_modelling.scripts.plot_srf_rakes:main"
plot-srf-rise = "source_modelling.scripts.plot_srf_rise:main"
plot-mw-contributions = "source_modelling.scripts.plot_mw_contributions:main"

[tool.setuptools.package-dir]
source_modelling = "source_modelling"

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}
